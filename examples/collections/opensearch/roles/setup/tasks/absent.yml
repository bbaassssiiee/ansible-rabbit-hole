---

- name: Remove namespaces
  kubernetes.core.k8s:
    name: "{{ item.key }}"
    api_version: v1
    kind: Namespace
    state: absent
    validate:
      fail_on_error: true
  loop: "{{ namespaces | dict2items }}"
  loop_control:
    label: '{{ item.key }}'
  when: item.value.create | bool
